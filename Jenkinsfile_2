pipeline {
    agent any
    tools {
        maven 'Maven 3.9.16'
    }
    stages {
        stage('Checkout y Compile') {
            steps {
                sh 'mvn clean compile'
            }
        }
    }
}
